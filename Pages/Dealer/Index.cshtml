@page
@model Twilio.Example.Pages.Dealer.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dealer[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dealer[0].PrivateNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dealer[0].LocalRegion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Dealer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrivateNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocalRegion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DealerId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DealerId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DealerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
